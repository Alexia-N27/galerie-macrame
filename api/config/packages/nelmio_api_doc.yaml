nelmio_api_doc:
    documentation:
        info:
            title: Galerie Macramé API
            description: API publique et admin (galeries & photos)
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc